# Default values for gitprime-app.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: nginx
  tag:
  pullPolicy: IfNotPresent

nameOverride: ""
fullnameOverride: "GitPrime Application Stack"

resources: {}

nodeSelector: {}

tolerations: []

affinity: {}
