apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jenkins-agent.fullname" . }}-test-connection"
  labels:
{{ include "jenkins-agent.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "jenkins-agent.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
