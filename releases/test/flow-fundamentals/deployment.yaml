---
apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: flow-fundamentals-test
  namespace: flow-fundamentals
  annotations:
    fluxcd.io/automated: "true"
spec:
  releaseName: flow-fundamentals-test
  chart:
    git: git@github.com:Git-Prime/gphelm-chart-repo.git
    path: charts/flow-fundamentals/
    version: 0.1.0
    ref: master
  values:
    image:
        tag:
